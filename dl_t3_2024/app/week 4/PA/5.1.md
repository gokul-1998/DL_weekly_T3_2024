- what is the problem with momentum-based Gradient Descent?
    - `Momentum-based Gradient Descent can overshoot the minima`, it oscillates around the minima a lot

- so what is the solution?
    - `Nesterov Accelerated Gradient Descent` is the solution